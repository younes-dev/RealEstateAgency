{{ form_start(form) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.title) }}</div>
    <div class="col-md-4">{{ form_row(form.surface) }}</div>
    <div class="col-md-4">{{ form_row(form.price) }}</div>
</div>
<div class="row">
    <div class="col-md-2">{{ form_row(form.rooms) }}</div>
    <div class="col-md-2">{{ form_row(form.bedrooms) }}</div>
    <div class="col-md-2">{{ form_row(form.airConditioner) }}</div>
    <div class="col-md-2">{{ form_row(form.floor) }}</div>
</div>
<div class="row">
    <div class="col-md-5">{{ form_row(form.adress) }}</div>
    <div class="col-md-2">{{ form_row(form.city) }}</div>
    <div class="col-md-2">{{ form_row(form.code_zip) }}</div>


    {# Todo : change this checkbox by Toggle :  #}
    {# Todo : https://www.w3schools.com/bootstrap4/tryit.asp?filename=trybs_form_custom_switch&stacked=h  #}
    <div class="col-md-2">{{ form_row(form.sold) }}</div>
</div>

<div class="row">
    <div class="col-md-10">{{ form_row(form.description) }}</div>
</div>

{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregister') }}</button>
{{ form_end(form) }}

